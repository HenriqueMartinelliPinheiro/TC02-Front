import React from 'react';

interface FormCourseProps {
  initialData?: { id?: number; name: string; status: number };
}

export const FormCourse: React.FC<FormCourseProps> = () => {

  return (
    <div className="max-w-sm mx-auto">
      <form>
        
      </form>
    </div>
  );
};
